<h2><%= group.escape("name") %></h2>
<h5>Members: <%= displayNames.join(", ") %> <button class="btn btn-sm btn-success" id="add-new-member-btn"><i class="fa fa-plus"></i></button></h5>
<button class="btn btn-danger btn-xs" id="leave-group-btn">Leave group</button>
<h3>Recently split bills</h3>
<table class="table table-condensed table-striped">
  <tr>
    <th class="col-md-2">Date</th>
    <th class="col-md-7">Bill</th>
    <th class="col-md-3">Total</th>
  </tr>
  <% bills.each(function (bill) { %>
    <tr>
      <td><%= moment(bill.created_at).format("D.MMM.YY") %></td>
      <td><%= bill.escape("description") %></td>
      <td><%= BT.int_to_dec(bill.get("total")) %></td>
    </tr>
  <% }) %>
</table>